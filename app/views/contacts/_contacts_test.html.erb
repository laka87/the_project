
</br>

<table id="reports" class="display">
  <thead>


      <th >Name</th>
      <th >Kontaktemail</th>
      <th >Telefon</th>
      <th colspan="3">Optionen</th>
    </tr>
  </thead>

  <tbody>
    <% @contacts.each do |contact| %>
    <% if current_user == contact.user %>
      <tr>
        <td ><%= contact.name %></td>
        <td ><%= contact.email %></td>

        <td ><%= link_to '+Kontaktpers', new_contact_contactpsn_path(contact), remote: true%></td>


        <td ><%= link_to 'Kontaktpers', contact_contactpsns_path(contact), remote: true %></td>



        <% end %>
      </tr>
  <% end %>
  </tbody>
</table>

<br>
